name: Github Action

on:
  push:
    branches:
      - master

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: commit
        run: echo ${{github.sha::6}}

      - shell: bash
        run: |
          expr 1 + 1 > output.log

      - name: "Upload output file"
        uses: actions/upload-artifact@v3
        with:
          name: output-log-file
          path: output.log